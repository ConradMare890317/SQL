CREATE TABLE chapter1_teachers (
	id bigserial,
	first_name varchar (30),
	last_name varchar (30),
	department_name varchar (30),
	job_title varchar (30),
	salary_figure numeric,
	overtime_hours time
);